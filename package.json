{
    "dependencies": {
        "compression": "^1.7.4",
        "express": "^4.17.1",
        "helmet": "^4.6.0"
    },
    "devDependencies": {
        "lwc-services": "^3.2.2"
    },
    "lwc": {
        "modules": [
            {
                "dir": "src/modules"
            }
        ]
    },
    "scripts": {
        "bs": "yarn build:dev && yarn serve",
        "build": "lwc-services build --mode production",
        "build:dev": "lwc-services build",
        "serve": "node --experimental-specifier-resolution=node server/server.js",
        "watch": "lwc-services watch",
        "watch:prod": "lwc-services watch --mode=production"
    }
}
